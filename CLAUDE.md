# open-webui-tools

Project documentation lives in `README.md` — read it before making changes here (project
overview, tool layout, the job database, testing, deploying a revision to OWUI, status notes).

This file is kept intentionally thin and just points there: Claude Code auto-loads `CLAUDE.md`
(not `README.md`) at the start of every session, which is the only reason it exists — so treat
`README.md` as the single source of truth and don't duplicate its content back into this file.

Job-database schema and design rationale specifically: `src/SQLITE_JOB_DB_HANDOFF.md`.
